<scope>标签定义了类包在项目的使用阶段。项目阶段包括： 编译，运行，测试和发布。目前有5种取值
* compile：默认值，表示该依赖在编译范围内，跟随着项目所有运行阶段，会随着项目一起打包发布
* provided：已提供范围，不会随项目打包发布，需要使用者提供依赖，如Servlet API
* runtime：运行时范围，项目编译时不会参与，在运行时才会使用，如JDBC驱动
* test：只在测试时会参与编译和运行
* system：和provided类似，但需要显示指定依赖路径，此时maven不会从仓库种搜索该依赖
* import：传统的方式继承父类的依赖使用<parent>标签，但只能实现单继承，若要使用多继承，则可以配合<dependencyManagement>标签和import标签取值实现